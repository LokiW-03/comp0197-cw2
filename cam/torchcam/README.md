install the following dependencies only for experiments

```sh
pip install torchcam
pip install -U git+https://github.com/qubvel-org/segmentation_models.pytorch
pip install lightning albumentations
```